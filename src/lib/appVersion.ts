/**
 * Version de l'application, injectée par Vite depuis package.json.
 * Utilisée pour comparer avec la dernière release GitHub.
 */
const APP_VERSION: string = __APP_VERSION__;

export default APP_VERSION;
